import * as React from "react"

const IndexPage = () => {
  return (
    <main>
      Hello Gatsby !!!!
    </main>
  )
}

export default IndexPage

export const Head = () => <title>Home Page</title>
